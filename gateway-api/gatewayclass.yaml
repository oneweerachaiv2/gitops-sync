# GatewayClass for APISIX Ingress Controller
# This defines APISIX as a Gateway API implementation
apiVersion: gateway.networking.k8s.io/v1
kind: GatewayClass
metadata:
  name: apisix
spec:
  controllerName: apisix.apache.org/apisix-ingress-controller
  description: "APISIX Gateway API Controller"

