# Patch to update APISIX Ingress Controller ConfigMap
# Note: In APISIX Ingress Controller 2.0.0, GatewayProxy is referenced via IngressClass,
# not via the ConfigMap's gateway_proxy field
apiVersion: v1
kind: ConfigMap
metadata:
  name: apisix-ingress-config
  namespace: apisix

